{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "patchy-config Helm chart values",
    "type": "object",
    "additionalProperties": false,
    "definitions": {
        "integration": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "spec"],
            "properties": {
                "name": {
                    "type": "string",
                    "minLength": 1
                },
                "spec": {
                    "$ref": "#/definitions/integrationSpec"
                }
            }
        },
        "forge": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "spec"],
            "properties": {
                "name": {
                    "type": "string",
                    "minLength": 1
                },
                "spec": {
                    "$ref": "#/definitions/forgeSpec"
                }
            }
        },
        "project": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "spec"],
            "properties": {
                "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 25
                },
                "spec": {
                    "$ref": "#/definitions/projectSpec"
                }
            }
        },
        "integrationSpec": {
            "description": "IntegrationSpec configures one external system. Exactly the provider block\nmatching spec.provider must be set (CEL-enforced) — integrations are\nstrongly typed; the generic provider is the deliberate escape hatch, typed\naround patchy's own HTTP contract rather than a vendor's.",
            "properties": {
                "aws": {
                    "description": "AWS is the aws provider block.",
                    "properties": {
                        "resourceTags": {
                            "description": "ResourceTags enables the tag-inventory enhancer.",
                            "properties": {
                                "configAggregator": {
                                    "description": "ConfigAggregator queries an AWS Config aggregator (SQL over recorded\nconfiguration items).",
                                    "properties": {
                                        "name": {
                                            "description": "Name of the configuration aggregator.",
                                            "minLength": 1,
                                            "type": "string"
                                        },
                                        "region": {
                                            "description": "Region the aggregator lives in.",
                                            "minLength": 1,
                                            "type": "string"
                                        }
                                    },
                                    "required": ["name", "region"],
                                    "type": "object",
                                    "additionalProperties": false
                                },
                                "enabled": {
                                    "description": "Enabled turns the enhancer capability on.",
                                    "type": "boolean"
                                },
                                "repositoryHost": {
                                    "description": "RepositoryHost is the forge host repositories named by tags live on;\nempty means github.com.",
                                    "type": "string"
                                },
                                "resourceExplorer": {
                                    "description": "ResourceExplorer searches an AWS Resource Explorer view.",
                                    "properties": {
                                        "viewARN": {
                                            "description": "ViewARN is the Resource Explorer view, e.g.\narn:aws:resource-explorer-2:eu-west-2:123456789012:view/org-view/<uuid>.\nThe region the view lives in is read from the ARN.",
                                            "pattern": "^arn:[a-z-]+:resource-explorer-2:[a-z0-9-]+:[0-9]{12}:view/.+$",
                                            "type": "string"
                                        }
                                    },
                                    "required": ["viewARN"],
                                    "type": "object",
                                    "additionalProperties": false
                                },
                                "tags": {
                                    "description": "Tags overrides the resource tag names the enhancer reads.",
                                    "properties": {
                                        "name": {
                                            "description": "Name label carries the repository name.",
                                            "type": "string"
                                        },
                                        "org": {
                                            "description": "Org label carries the repository owner/organization.",
                                            "type": "string"
                                        },
                                        "provider": {
                                            "description": "Provider label carries the forge kind (e.g. github).",
                                            "type": "string"
                                        },
                                        "url": {
                                            "description": "URL label carries a full repository URL, overriding the parts.",
                                            "type": "string"
                                        }
                                    },
                                    "type": "object",
                                    "additionalProperties": false
                                }
                            },
                            "required": ["enabled"],
                            "type": "object",
                            "additionalProperties": false
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                },
                "azure": {
                    "description": "Azure is the azure provider block.",
                    "properties": {
                        "resourceTags": {
                            "description": "ResourceTags enables the tag-inventory enhancer.",
                            "properties": {
                                "enabled": {
                                    "description": "Enabled turns the enhancer capability on.",
                                    "type": "boolean"
                                },
                                "managementGroup": {
                                    "description": "ManagementGroup narrows the query scope to one management group; empty\nmeans every subscription the ambient identity can read (the tenant, in\npractice).",
                                    "type": "string"
                                },
                                "repositoryHost": {
                                    "description": "RepositoryHost is the forge host repositories named by tags live on;\nempty means github.com.",
                                    "type": "string"
                                },
                                "tags": {
                                    "description": "Tags overrides the resource tag names the enhancer reads.",
                                    "properties": {
                                        "name": {
                                            "description": "Name label carries the repository name.",
                                            "type": "string"
                                        },
                                        "org": {
                                            "description": "Org label carries the repository owner/organization.",
                                            "type": "string"
                                        },
                                        "provider": {
                                            "description": "Provider label carries the forge kind (e.g. github).",
                                            "type": "string"
                                        },
                                        "url": {
                                            "description": "URL label carries a full repository URL, overriding the parts.",
                                            "type": "string"
                                        }
                                    },
                                    "type": "object",
                                    "additionalProperties": false
                                }
                            },
                            "required": ["enabled"],
                            "type": "object",
                            "additionalProperties": false
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                },
                "backfill": {
                    "description": "Backfill requests one bounded list-alerts walk of the provider's open\nalerts, ingesting the ones that predate webhook coverage — alerts\nraised before the App was installed on (or associated with) their\nrepository, which no replay of the delivery log can ever surface.\nIngestion is idempotent, so re-listing alerts that already have\nfindings is safe. Freshness against status.backfill.backfilledAt\ndecides whether the request is still actionable — the controller\nnever clears spec.",
                    "properties": {
                        "at": {
                            "description": "At is when the request was received.",
                            "format": "date-time",
                            "type": "string"
                        },
                        "by": {
                            "description": "By is the requesting login.",
                            "type": "string"
                        },
                        "repositories": {
                            "description": "Repositories narrows the walk to repositories matching any entry: an\nexact \"owner/name\", or an \"owner/\" prefix covering the whole owner.\nEmpty means the credential's full scope. A truncated backfill re-runs\nwith a narrower prefix rather than paging on.",
                            "items": {
                                "maxLength": 200,
                                "minLength": 1,
                                "type": "string"
                            },
                            "maxItems": 50,
                            "type": "array"
                        }
                    },
                    "required": ["at", "by"],
                    "type": "object",
                    "additionalProperties": false
                },
                "generic": {
                    "description": "Generic is the generic provider block.",
                    "properties": {
                        "enhance": {
                            "description": "Enhance enables the outbound context enhancer.",
                            "properties": {
                                "enabled": {
                                    "description": "Enabled turns the enhancer capability on.",
                                    "type": "boolean"
                                },
                                "timeout": {
                                    "default": "60s",
                                    "description": "Timeout bounds each call.",
                                    "type": "string"
                                },
                                "url": {
                                    "description": "URL patchy POSTs enhancement requests to.",
                                    "minLength": 1,
                                    "pattern": "^https?://.+$",
                                    "type": "string"
                                }
                            },
                            "required": ["enabled", "url"],
                            "type": "object",
                            "additionalProperties": false
                        },
                        "source": {
                            "description": "Source enables the inbound findings webhook.",
                            "properties": {
                                "enabled": {
                                    "description": "Enabled turns the inbound receiver on for this integration.",
                                    "type": "boolean"
                                },
                                "minSeverity": {
                                    "default": "low",
                                    "description": "MinSeverity drops findings below this severity at ingestion.",
                                    "enum": ["low", "medium", "high", "critical"],
                                    "type": "string"
                                },
                                "resolver": {
                                    "description": "Resolver enables verdict write-back to the external process.",
                                    "properties": {
                                        "enabled": {
                                            "description": "Enabled turns the write-back on.",
                                            "type": "boolean"
                                        },
                                        "timeout": {
                                            "default": "60s",
                                            "description": "Timeout bounds each call.",
                                            "type": "string"
                                        },
                                        "url": {
                                            "description": "URL patchy POSTs verdicts to.",
                                            "minLength": 1,
                                            "pattern": "^https?://.+$",
                                            "type": "string"
                                        }
                                    },
                                    "required": ["enabled", "url"],
                                    "type": "object",
                                    "additionalProperties": false
                                }
                            },
                            "required": ["enabled"],
                            "type": "object",
                            "additionalProperties": false
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                },
                "github": {
                    "description": "GitHub is the github provider block.",
                    "properties": {
                        "baseURL": {
                            "description": "BaseURL points at a GitHub Enterprise Server host; empty means\ngithub.com.",
                            "type": "string"
                        },
                        "codeScanningAlerts": {
                            "description": "CodeScanningAlerts enables scanner ingestion.",
                            "properties": {
                                "enabled": {
                                    "description": "Enabled turns the code-scanning capability on.",
                                    "type": "boolean"
                                }
                            },
                            "required": ["enabled"],
                            "type": "object",
                            "additionalProperties": false
                        },
                        "issues": {
                            "description": "Issues enables the tracking projection.",
                            "properties": {
                                "approveComment": {
                                    "default": "/approve",
                                    "description": "ApproveComment is the deprecated alias of \"/patchy approve\" on a\nfinding's tracking issue: a comment that is exactly it, or starts\nwith it and a space, approves as \"/patchy approve\" does, with the same\nwrite-access check and reply. \"/patchy <verb>\" is the command grammar\nevery tracking issue accepts whatever this says.",
                                    "type": "string"
                                },
                                "enabled": {
                                    "description": "Enabled turns the issues capability on.",
                                    "type": "boolean"
                                },
                                "fallbackRepository": {
                                    "description": "FallbackRepository (\"owner/repo\") receives the tracking issues of\nfindings that have no repository of their own — cloud findings whose\nresource carries no ownership labels. Without it those findings are\nvisible only through kubectl and the status page. They still cannot be\nremediated: the issue is a human surface, not a work tree.",
                                    "type": "string"
                                }
                            },
                            "required": ["enabled"],
                            "type": "object",
                            "additionalProperties": false
                        },
                        "proxy": {
                            "description": "Proxy routes this integration's GitHub traffic through an HTTP/HTTPS\nforward proxy, overriding the process proxy environment for this\nintegration.",
                            "properties": {
                                "url": {
                                    "description": "URL of the proxy, e.g. \"http://proxy.corp.example:3128\". Schemes http\nand https. Credentials must not be embedded in the URL — put them in\nthe credential Secret under \"proxyUsername\" + \"proxyPassword\".",
                                    "minLength": 1,
                                    "pattern": "^https?://[^@\\s]+$",
                                    "type": "string"
                                }
                            },
                            "required": ["url"],
                            "type": "object",
                            "additionalProperties": false
                        },
                        "redelivery": {
                            "description": "Redelivery enables the failed-delivery sweep.",
                            "properties": {
                                "enabled": {
                                    "description": "Enabled turns the sweep on. Requires App credentials — the delivery\nlog is App-scoped, so a PAT integration cannot sweep.",
                                    "type": "boolean"
                                },
                                "lookback": {
                                    "default": "24h",
                                    "description": "Lookback bounds how far back each sweep scans the delivery log.\nGitHub retains deliveries for 30 days.",
                                    "type": "string"
                                }
                            },
                            "required": ["enabled"],
                            "type": "object",
                            "additionalProperties": false
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                },
                "googleCloud": {
                    "description": "GoogleCloud is the google-cloud provider block.",
                    "properties": {
                        "cloudAssetInventory": {
                            "description": "CloudAssetInventory enables the ownership-label enhancer.",
                            "properties": {
                                "enabled": {
                                    "description": "Enabled turns the enhancer capability on.",
                                    "type": "boolean"
                                },
                                "labels": {
                                    "description": "Labels overrides the resource label names the enhancer reads.",
                                    "properties": {
                                        "name": {
                                            "description": "Name label carries the repository name.",
                                            "type": "string"
                                        },
                                        "org": {
                                            "description": "Org label carries the repository owner/organization.",
                                            "type": "string"
                                        },
                                        "provider": {
                                            "description": "Provider label carries the forge kind (e.g. github).",
                                            "type": "string"
                                        },
                                        "url": {
                                            "description": "URL label carries a full repository URL, overriding the parts.",
                                            "type": "string"
                                        }
                                    },
                                    "type": "object",
                                    "additionalProperties": false
                                },
                                "repositoryHost": {
                                    "description": "RepositoryHost is the forge host repositories named by labels live on;\nempty means github.com.",
                                    "type": "string"
                                },
                                "scope": {
                                    "description": "Scope bounds the asset search: an organization, folder, or project.",
                                    "pattern": "^(organizations|folders|projects)/.+$",
                                    "type": "string"
                                }
                            },
                            "required": ["enabled", "scope"],
                            "type": "object",
                            "additionalProperties": false
                        },
                        "securityCommandCenter": {
                            "description": "SecurityCommandCenter enables SCC finding ingestion.",
                            "properties": {
                                "audience": {
                                    "description": "Audience the push subscription's OIDC token must carry, as configured\non the subscription's oidcToken.audience.",
                                    "minLength": 1,
                                    "type": "string"
                                },
                                "enabled": {
                                    "description": "Enabled turns the SCC ingestion capability on.",
                                    "type": "boolean"
                                },
                                "minSeverity": {
                                    "default": "low",
                                    "description": "MinSeverity drops notifications below this severity before they become\nfindings. The notification filter should do most of this work; this is\nthe backstop.",
                                    "enum": ["low", "medium", "high", "critical"],
                                    "type": "string"
                                },
                                "mute": {
                                    "description": "Mute configures the ignore-verdict write-back (reserved).",
                                    "properties": {
                                        "enabled": {
                                            "description": "Enabled turns the mute-on-ignore write-back on.",
                                            "type": "boolean"
                                        }
                                    },
                                    "required": ["enabled"],
                                    "type": "object",
                                    "additionalProperties": false
                                },
                                "organization": {
                                    "description": "Organization is the numeric Google Cloud organization id, used to\ncompose Console links for findings that carry no externalUri.",
                                    "type": "string"
                                },
                                "serviceAccount": {
                                    "description": "ServiceAccount is the email address the push token must be issued for;\na token from any other identity is rejected.",
                                    "minLength": 1,
                                    "type": "string"
                                }
                            },
                            "required": ["audience", "enabled", "serviceAccount"],
                            "type": "object",
                            "additionalProperties": false
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                },
                "interval": {
                    "default": "10m",
                    "description": "Interval between credential revalidations.",
                    "type": "string"
                },
                "provider": {
                    "description": "Provider is the external system type.",
                    "enum": ["github", "google-cloud", "wiz", "aws", "azure", "generic"],
                    "type": "string"
                },
                "replay": {
                    "description": "Replay requests one full redelivery of every webhook delivery in the\nlookback window, including deliveries that already succeeded (the\nreceiver dedups; ingestion is idempotent). Freshness against\nstatus.redelivery.replayedAt decides whether the request is still\nactionable — the controller never clears spec.",
                    "properties": {
                        "at": {
                            "description": "At is when the request was received.",
                            "format": "date-time",
                            "type": "string"
                        },
                        "by": {
                            "description": "By is the requesting login.",
                            "type": "string"
                        }
                    },
                    "required": ["at", "by"],
                    "type": "object",
                    "additionalProperties": false
                },
                "reset": {
                    "description": "Reset requests the demo reset (stamped by the status page): the\ncontroller permanently deletes every finding's tracking issue,\nreopens the dismissed findings' code-scanning alerts, deletes every\npipeline resource, and drops the receiver's delivery dedup window so\nredeliveries of already-handled GUIDs are ingested again. Issue\ndeletion requires admin-level repository permission on the\ncredential. Freshness against status.resetAt decides whether the\nrequest is still actionable — the controller never clears spec.",
                    "properties": {
                        "at": {
                            "description": "At is when the request was received.",
                            "format": "date-time",
                            "type": "string"
                        },
                        "by": {
                            "description": "By is the requesting login.",
                            "type": "string"
                        }
                    },
                    "required": ["at", "by"],
                    "type": "object",
                    "additionalProperties": false
                },
                "secretRef": {
                    "description": "SecretRef names the credential Secret. For github: either key \"token\"\n(PAT, dev) or keys \"appID\" + \"privateKey\" (GitHub App), plus\n\"webhookSecret\" for receiver HMAC validation, plus optional keys\n\"proxyUsername\" + \"proxyPassword\" when spec.github.proxy needs basic\nauth. For wiz: key\n\"webhookToken\" (the shared bearer token deliveries carry), plus keys\n\"clientId\" + \"clientSecret\" when spec.wiz.api enables write-back. For\ngeneric: key \"webhookSecret\", the shared HMAC secret signing both\ninbound deliveries and patchy's outbound resolver/enhancer calls.\nRequired for github, wiz, and generic, optional otherwise — a\ngoogle-cloud integration holds no credential, since Pub/Sub\nauthenticates itself with a signed OIDC token. It stays permitted for\nevery provider so a future capability that does need one (SCC\nmute-on-ignore) needs no schema change.",
                    "properties": {
                        "name": {
                            "description": "Name of the Secret.",
                            "minLength": 1,
                            "type": "string"
                        }
                    },
                    "required": ["name"],
                    "type": "object",
                    "additionalProperties": false
                },
                "suspend": {
                    "description": "Suspend pauses reconciliation of this integration.",
                    "type": "boolean"
                },
                "wiz": {
                    "description": "Wiz is the wiz provider block.",
                    "properties": {
                        "api": {
                            "description": "API enables write-back through the Wiz GraphQL API.",
                            "properties": {
                                "endpoint": {
                                    "description": "Endpoint is the tenant GraphQL endpoint, e.g.\nhttps://api.eu1.app.wiz.io/graphql.",
                                    "minLength": 1,
                                    "type": "string"
                                },
                                "tokenURL": {
                                    "description": "TokenURL is the OAuth2 client-credentials token endpoint; empty means\nhttps://auth.app.wiz.io/oauth/token.",
                                    "type": "string"
                                }
                            },
                            "required": ["endpoint"],
                            "type": "object",
                            "additionalProperties": false
                        },
                        "defend": {
                            "description": "Defend enables Wiz Defend threat-detection ingestion.",
                            "properties": {
                                "enabled": {
                                    "description": "Enabled turns the Wiz Defend ingestion capability on.",
                                    "type": "boolean"
                                },
                                "minSeverity": {
                                    "default": "low",
                                    "description": "MinSeverity drops detections below this severity before they become\nfindings.",
                                    "enum": ["low", "medium", "high", "critical"],
                                    "type": "string"
                                }
                            },
                            "required": ["enabled"],
                            "type": "object",
                            "additionalProperties": false
                        },
                        "issues": {
                            "description": "Issues enables Wiz Issues ingestion.",
                            "properties": {
                                "enabled": {
                                    "description": "Enabled turns the Wiz Issues ingestion capability on.",
                                    "type": "boolean"
                                },
                                "minSeverity": {
                                    "default": "low",
                                    "description": "MinSeverity drops issues below this severity before they become\nfindings. Wiz INFORMATIONAL ranks below low, so the default floor\ndrops it.",
                                    "enum": ["low", "medium", "high", "critical"],
                                    "type": "string"
                                }
                            },
                            "required": ["enabled"],
                            "type": "object",
                            "additionalProperties": false
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                }
            },
            "required": ["provider"],
            "type": "object",
            "additionalProperties": false
        },
        "forgeSpec": {
            "description": "ForgeSpec configures repository credentials for one forge, scoped by\noptional org and repository filters. Matching (in internal/forge): the\nrepository URL's host must equal the forge host, then the org allowlist,\nthen the repository regexes; when several Forges match, the more\nconstrained spec wins (orgs set beats unset, then repositories set beats\nunset) and a remaining tie is a configuration error surfaced on the\nFinding.",
            "properties": {
                "baseURL": {
                    "description": "BaseURL is the forge host; empty means the provider's public host\n(github.com).",
                    "type": "string"
                },
                "interval": {
                    "default": "10m",
                    "description": "Interval between credential revalidations.",
                    "type": "string"
                },
                "orgs": {
                    "description": "Orgs restricts this forge to the listed organizations; empty means all.",
                    "items": {
                        "type": "string"
                    },
                    "type": "array"
                },
                "provider": {
                    "description": "Provider is the forge family.",
                    "enum": ["github"],
                    "type": "string"
                },
                "proxy": {
                    "description": "Proxy routes this forge's GitHub traffic through an HTTP/HTTPS forward\nproxy, overriding the process proxy environment for this forge.",
                    "properties": {
                        "url": {
                            "description": "URL of the proxy, e.g. \"http://proxy.corp.example:3128\". Schemes http\nand https. Credentials must not be embedded in the URL — put them in\nthe credential Secret under \"proxyUsername\" + \"proxyPassword\".",
                            "minLength": 1,
                            "pattern": "^https?://[^@\\s]+$",
                            "type": "string"
                        }
                    },
                    "required": ["url"],
                    "type": "object",
                    "additionalProperties": false
                },
                "repositories": {
                    "description": "Repositories restricts this forge to repositories whose \"owner/name\"\nmatches any of the listed RE2 regular expressions; empty means all.",
                    "items": {
                        "type": "string"
                    },
                    "type": "array"
                },
                "secretRef": {
                    "description": "SecretRef names the credential Secret: key \"token\" (PAT, dev) or keys\n\"appID\" + \"privateKey\" (GitHub App). With spec.proxy set, optional keys\n\"proxyUsername\" + \"proxyPassword\" carry the proxy's basic-auth\ncredentials.",
                    "properties": {
                        "name": {
                            "description": "Name of the Secret.",
                            "minLength": 1,
                            "type": "string"
                        }
                    },
                    "required": ["name"],
                    "type": "object",
                    "additionalProperties": false
                },
                "suspend": {
                    "description": "Suspend pauses reconciliation of this forge.",
                    "type": "boolean"
                }
            },
            "required": ["provider", "secretRef"],
            "type": "object",
            "additionalProperties": false
        },
        "projectSpec": {
            "description": "ProjectSpec is operator configuration: where intents are filed, who may\nauthorise them, which repositories they build in, and what they may spend.\nThe operator writes it (through the patchy-config chart); intent-controller\nonly reads it. Writing projects is admin-only in RBAC, because a Project is\nthe power to point agents at repositories.",
            "properties": {
                "approvers": {
                    "description": "Approvers is the allowlist whose actions count.",
                    "properties": {
                        "logins": {
                            "description": "Logins are the GitHub logins whose trigger, approval, commands and\nreview feedback count; everyone else's are ignored (a trigger from\nanyone else closes the intent with one notice). Compared\ncase-insensitively. A Bot never counts, so the pattern admits no\n\"[bot]\" suffix. The operator owns this list; it is never derived from\nauthor_association.",
                            "items": {
                                "maxLength": 64,
                                "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$",
                                "type": "string"
                            },
                            "maxItems": 20,
                            "minItems": 1,
                            "type": "array",
                            "x-kubernetes-list-type": "set"
                        }
                    },
                    "required": ["logins"],
                    "type": "object",
                    "additionalProperties": false
                },
                "checks": {
                    "default": {},
                    "description": "Checks configures automatic check-fix rounds (slice 1b).",
                    "properties": {
                        "fix": {
                            "description": "Fix names the checks (check-run names or commit-status contexts, e.g.\n\"test\", \"lint\") whose failure on patchy's own head starts a fix round\nwithout a human; the round records the failed check runs and commit\nstatuses it consumed, each by its own id (IntentRunInputs.CheckRunIDs\nand StatusIDs). Empty means patchy never auto-fixes: a failing check is\nreported on the intent issue and an approver can run /patchy revise.\nThe list is explicit so a flaky or unrelated check cannot burn\nbudget.",
                            "items": {
                                "maxLength": 128,
                                "minLength": 1,
                                "type": "string"
                            },
                            "maxItems": 32,
                            "type": "array",
                            "x-kubernetes-list-type": "set"
                        },
                        "timeout": {
                            "default": "30m",
                            "description": "Timeout is how long after a push the controller waits for the named\nchecks to settle before it stops watching them, between 1m and 6h. A\npointer so that an unset value is omitted and the schema default\napplies (a zero metav1.Duration would serialize as \"0s\").",
                            "type": "string"
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                },
                "intentRepository": {
                    "description": "IntentRepository is the https URL of the repository whose issues are\nthis Project's intents (e.g. https://github.com/acme/intents). Several\nProjects may share one, told apart by their trigger labels. The App\nmust be installed on it and it must resolve to exactly one Forge. It\nis immutable (CEL-enforced): an Intent is named <project>-<issue>,\nwhich leaves the repository out, so a Project moved to another intent\nrepository would find its new issues' names held by the old\nrepository's Intents. To move a project, create a new Project, under a\nnew name, on the new repository.",
                    "maxLength": 256,
                    "minLength": 1,
                    "pattern": "^https://[^/\\s@?#]+/[^/\\s?#]+/[^/\\s?#]+$",
                    "type": "string"
                },
                "labels": {
                    "default": {},
                    "description": "Labels names the trigger and approve labels.",
                    "properties": {
                        "approve": {
                            "default": "patchy:approved",
                            "description": "Approve is the label an approver adds to approve the plan posted on\nthe intent issue — the alias of `/patchy approve`. The approval is\naccepted only from an approver, after the plan was posted, and while\nboth the plan comment and the issue body still hash to what was\nplanned.",
                            "maxLength": 50,
                            "minLength": 1,
                            "type": "string"
                        },
                        "trigger": {
                            "description": "Trigger is the label that both starts an intent and names this\nProject: an issue carrying it becomes the Intent <project>-<issue>,\nand an approver re-applying it asks for a replan. Empty means\n\"patchy:<project name>\" (ProjectTriggerLabel), which intent-controller\nderives because a schema default cannot see the object's name. The\nname budget keeps it within GitHub's label limit. A root-level rule\nrefuses a derived trigger that equals the approve label, so a Project\nnamed \"approved\" must set this, or a different approve label. Two\nProjects sharing an intent repository must use different triggers.\n50 characters is GitHub's own label-name limit.",
                            "maxLength": 50,
                            "minLength": 1,
                            "type": "string"
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                },
                "limits": {
                    "default": {},
                    "description": "Limits bound each intent's spend.",
                    "properties": {
                        "build": {
                            "description": "Build bounds each initial build run.",
                            "properties": {
                                "maxTurns": {
                                    "description": "MaxTurns the agent may take in one run of the stage.",
                                    "format": "int32",
                                    "maximum": 1000,
                                    "minimum": 0,
                                    "type": "integer"
                                },
                                "tokenBudget": {
                                    "description": "TokenBudget is the output-token budget of one run of the stage (the\nrunner's kill switch).",
                                    "format": "int64",
                                    "maximum": 100000000,
                                    "minimum": 0,
                                    "type": "integer"
                                }
                            },
                            "type": "object",
                            "additionalProperties": false
                        },
                        "maxActiveIntents": {
                            "default": 2,
                            "description": "MaxActiveIntents is how many of this Project's intents may be\nnon-terminal at once; a newly triggered issue past it waits.",
                            "format": "int32",
                            "maximum": 20,
                            "minimum": 1,
                            "type": "integer"
                        },
                        "maxCheckFixes": {
                            "default": 2,
                            "description": "MaxCheckFixes is how many automatic check-fix rounds one intent may\nrun, counted separately from MaxRevisions but under the same cost\nceiling (slice 1b). Zero is meaningful (no check-fix rounds), so the\nfield is a pointer: nil means the default.",
                            "format": "int32",
                            "maximum": 20,
                            "minimum": 0,
                            "type": "integer"
                        },
                        "maxCostMicroUSD": {
                            "default": 10000000,
                            "description": "MaxCostMicroUSD is the per-intent spend ceiling in micro-USD (10000000\n= $10), checked before every launch; past it the Intent goes Blocked\nwith BudgetExhausted. Advisory for build and revise runs, whose\nreported usage comes from a repository-declared image and is\nuntrusted — the per-run limits, the Job deadline and MaxRevisions are\nwhat is actually enforced there. The ceiling is capped at $1000.",
                            "format": "int64",
                            "maximum": 1000000000,
                            "minimum": 1,
                            "type": "integer"
                        },
                        "maxRevisions": {
                            "default": 3,
                            "description": "MaxRevisions is how many review-driven revise rounds one intent may\nrun; the next is refused and the Intent goes Blocked with\nRevisionLimitReached until the limit is raised. A head_moved re-run\ndoes not count. Zero is meaningful (no revise rounds), so the field\nis a pointer: nil means the default.",
                            "format": "int32",
                            "maximum": 20,
                            "minimum": 0,
                            "type": "integer"
                        },
                        "plan": {
                            "description": "Plan bounds each planning run (read-only, default runner image).",
                            "properties": {
                                "maxTurns": {
                                    "description": "MaxTurns the agent may take in one run of the stage.",
                                    "format": "int32",
                                    "maximum": 1000,
                                    "minimum": 0,
                                    "type": "integer"
                                },
                                "tokenBudget": {
                                    "description": "TokenBudget is the output-token budget of one run of the stage (the\nrunner's kill switch).",
                                    "format": "int64",
                                    "maximum": 100000000,
                                    "minimum": 0,
                                    "type": "integer"
                                }
                            },
                            "type": "object",
                            "additionalProperties": false
                        },
                        "revise": {
                            "description": "Revise bounds each revise or check-fix run.",
                            "properties": {
                                "maxTurns": {
                                    "description": "MaxTurns the agent may take in one run of the stage.",
                                    "format": "int32",
                                    "maximum": 1000,
                                    "minimum": 0,
                                    "type": "integer"
                                },
                                "tokenBudget": {
                                    "description": "TokenBudget is the output-token budget of one run of the stage (the\nrunner's kill switch).",
                                    "format": "int64",
                                    "maximum": 100000000,
                                    "minimum": 0,
                                    "type": "integer"
                                }
                            },
                            "type": "object",
                            "additionalProperties": false
                        }
                    },
                    "type": "object",
                    "additionalProperties": false
                },
                "repositories": {
                    "description": "Repositories are the application repositories intents build in; the\nfirst entry is also the planning repository. The schema admits up to\n8 — the multi-repo shape — but slice 1 builds in exactly one:\nintent-controller reports a Project with more as not Ready rather\nthan the schema refusing it, so the CRD does not change when\nmulti-repo intents land. Each entry names a different repository:\nthe Intent records its pull requests by repository URL, and every\nrepository's branch is the same patchy-intent/<intent>, so two\nentries for one repository would fan two builds into one branch. URLs\nare compared the way forges compare them — case-insensitively, with\nany .git suffix dropped.",
                    "items": {
                        "description": "ProjectRepository is one application repository the Project's intents may\nbuild in.",
                        "properties": {
                            "name": {
                                "description": "Name is the repository's short key within the Project, used in the\nnames of the IntentRuns and Repositories created for it\n(<intent>-bld-r<round>-<name>-a<n>), hence a DNS label of at most 16\ncharacters (MaxRepositoryKeyLength; see the name budget).",
                                "maxLength": 16,
                                "minLength": 1,
                                "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
                                "type": "string"
                            },
                            "url": {
                                "description": "URL is the repository's https URL (https://github.com/<owner>/<name>).\nIt must resolve to exactly one Forge, and the App must be installed on\nit, or the Project is not Ready. No credentials in the URL.",
                                "maxLength": 256,
                                "minLength": 1,
                                "pattern": "^https://[^/\\s@?#]+/[^/\\s?#]+/[^/\\s?#]+$",
                                "type": "string"
                            }
                        },
                        "required": ["name", "url"],
                        "type": "object",
                        "additionalProperties": false
                    },
                    "maxItems": 8,
                    "minItems": 1,
                    "type": "array",
                    "x-kubernetes-list-map-keys": ["name"],
                    "x-kubernetes-list-type": "map"
                },
                "requireRepositoryImage": {
                    "default": true,
                    "description": "RequireRepositoryImage, true by default, launches build and revise\nruns only on an accepted repository-declared runner image (the\nRepository's pinned, not-rejected image) and blocks the Intent with\nImageRequired otherwise. False lets them fall back to the default\nrunner image, which carries no toolchain.",
                    "type": "boolean"
                },
                "suspend": {
                    "description": "Suspend stops the Project: no new intents are discovered and no run\nof its intents is launched. Running Jobs finish.",
                    "type": "boolean"
                }
            },
            "required": ["approvers", "intentRepository", "repositories"],
            "type": "object",
            "additionalProperties": false
        }
    },
    "properties": {
        "global": {
            "type": "object"
        },
        "nameOverride": {
            "type": "string"
        },
        "fullnameOverride": {
            "type": "string"
        },
        "commonLabels": {
            "type": "object"
        },
        "commonAnnotations": {
            "type": "object"
        },
        "integrations": {
            "type": "array",
            "description": "Integration custom resources rendered into the release namespace",
            "items": {
                "$ref": "#/definitions/integration"
            }
        },
        "forges": {
            "type": "array",
            "description": "Forge custom resources rendered into the release namespace",
            "items": {
                "$ref": "#/definitions/forge"
            }
        },
        "projects": {
            "type": "array",
            "description": "Project custom resources (intent-driven development) rendered into the release namespace",
            "items": {
                "$ref": "#/definitions/project"
            }
        }
    }
}
