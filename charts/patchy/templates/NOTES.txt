patchy {{ .Chart.AppVersion }} deployed to namespace {{ .Release.Namespace }}
(agent sandbox: {{ .Values.agent.namespace }}).

The CRs are the state machine: findings, investigations, and remediations
live as patchy.bitwisemedia.uk custom resources in {{ .Release.Namespace }}
(kubectl get patchy -n {{ .Release.Namespace }}).

The pipeline idles until two things exist:

  1. An Integration and a Forge custom resource (render them with the
     `integrations:` / `forges:` values arrays, or apply your own). The
     Integration validates webhooks and projects tracking issues; the Forge
     clones and pushes repositories.
  2. The Secrets they reference, created out of band:
       * the forge/provider credential (keys: appID + privateKey, or token,
         plus webhookSecret on Integration secrets) in namespace
         {{ .Release.Namespace }}, named by each CR's spec.secretRef.
       * the model credentials. Claude's lives with the egress broker in
         namespace {{ .Release.Namespace }} ({{ .Values.egressBroker.anthropicSecret }}, key api-key, when the
         provider is anthropic; for bedrock/vertex/foundry-entra the broker
         uses its workload identity instead — annotate
         egressBroker.serviceAccount). The non-brokered runners, whether
         enabled for findings or for evaluations, each need a Secret in
         namespace {{ .Values.agent.namespace }}
         ({{- range $id, $r := .Values.agent.runners }}{{ if and (include "patchy.harnessEnabled" (dict "root" $ "id" $id)) $r.secret }} {{ $r.secret }} (key {{ $r.secretKey }}, as {{ $r.secretEnv }}){{ end }}{{- end }} — none needed when claude is the only harness enabled).
         A non-brokered harness is enabled only when its runner is enabled
         AND its credential exists; claude is enabled by configuration, with
         a missing broker credential surfacing at the broker's /readyz.
         Every allowlisted model needs one enabled harness that supports it,
         or the controllers refuse to start.
     See deploy/kustomize/base/secrets.example.yaml in the patchy repository
     for the exact shapes and kubectl one-liners.
{{- if or (index .Values.agent.runners.claude "secret") (index .Values.agent.runners.claude "secretKey") (index .Values.agent.runners.claude "secretEnv") }}

WARNING: agent.runners.claude.secret / secretKey / secretEnv are set but
IGNORED — claude runs proxy-only through the egress broker and its pods hold
no credential. Move the Anthropic credential to a Secret named
`{{ .Values.egressBroker.anthropicSecret }}` (key api-key) in namespace {{ .Release.Namespace }}, delete the old
Secret from {{ .Values.agent.namespace }}, and drop these values. If the old
secretEnv was CLAUDE_CODE_OAUTH_TOKEN (a `claude setup-token` token), also
set egressBroker.anthropicAuth=token so the broker sends it as a bearer.
{{- end }}
{{- if not (and .Values.integrations .Values.forges) }}

NOTE: {{ if not .Values.integrations }}`integrations` is empty{{ if not .Values.forges }} and {{ end }}{{ end }}{{ if not .Values.forges }}`forges` is empty{{ end }} — no findings will be
ingested{{ if not .Values.forges }} and no repository can be cloned{{ end }} until the missing CR(s) exist.
{{- end }}

{{- if or .Values.webhook.ingress.enabled .Values.webhook.httpRoute.enabled }}

Set the GitHub App's (single) webhook URL to:

  https://{{ .Values.webhook.host }}/github/webhooks

The integration-controller behind it validates each delivery against the
matching Integration's webhookSecret.
{{- else }}

No webhook entry point is enabled. Expose the integration-controller (the
only internet-facing component; it validates each delivery against the
matching Integration's webhookSecret) with webhook.host plus
webhook.ingress.enabled or webhook.httpRoute.enabled — or bring your own
routing to the {{ include "patchy.fullname" . }}-integration-controller
Service — then point the GitHub App's single webhook URL at
https://<host>/github/webhooks.
{{- end }}
{{- if .Values.intentController.enabled }}

intent-controller is enabled. It does nothing until a Project exists: add
one per project to the patchy-config chart's `projects` array, then check
`kubectl get projects -n {{ .Release.Namespace }}` reports Ready. It reads only
the Forge Secrets named in intentController.forgeSecrets in the release namespace
({{ join ", " .Values.intentController.forgeSecrets }}): the Projects of a Forge referencing any other Secret report
Ready=False with reason ForgeSecretUnreadable, naming the Secret.
Its agent-jobs Role can get, create, update and delete any Secret in the agents namespace, including model keys,
image-pull credentials and other Jobs' handoffs.
{{- if not (.Values.agent.repositoryImages | default dict).enabled }}

NOTE: agent.repositoryImages is off, so every intent build blocks on
ImageRequired unless its Project sets requireRepositoryImage: false. Builds
are meant to run in the application repository's own image.
{{- end }}
{{- end }}
{{- $egressMode := include "patchy.egressMode" . }}
{{- if eq $egressMode "none" }}

NOTE: the agent sandbox currently has no hostname-level egress policy — the
base NetworkPolicy allows TCP 443 to anywhere, and credential absence (the
agent pod holds no forge credential; the repository arrives as a
digest-verified tarball from source-controller's artifact server) is the
control.

agent.networkPolicy.mode is {{ .Values.agent.networkPolicy.mode | default "auto" }}, and no enforceable dialect was found.
For defence in depth, one of:

  * GKE Dataplane V2 — recreate or update the cluster with
    --enable-fqdn-network-policy, then re-reconcile; mode auto picks it up.
  * Cilium — needs DNS-based policy (the DNS proxy); auto picks it up.
  * Istio — set mode: istio (never auto-selected: it needs native sidecars
    and namespace injection, which CRD presence does not imply).

Note that auto can only see the cluster when Helm renders against one; an
off-cluster `helm template` always reports none.
{{- else }}

Agent sandbox egress: hostname-enforced via mode {{ $egressMode }}{{ if include "patchy.broadEgress" . }}, but the base
policy's broad "TCP 443 to anywhere" rule is STILL IN PLACE — network policies
are additive, so the allowlist is not actually constraining egress. That is
what agent.networkPolicy.broadEgress={{ .Values.agent.networkPolicy.broadEgress | default "auto" }} asks for; set it to `auto` once the mode
is soaked{{ else }} — each runner reaches only its own model API{{ end }}.
{{- end }}

For the pod-security labels on {{ .Release.Namespace }} (the chart only
labels the agent namespace it creates):

  kubectl label ns {{ .Release.Namespace }} \
    pod-security.kubernetes.io/enforce=restricted \
    pod-security.kubernetes.io/audit=restricted \
    pod-security.kubernetes.io/warn=restricted
