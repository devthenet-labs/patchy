# Copyright 2026 Bitwise Media Group Ltd.
# SPDX-License-Identifier: MIT
#
# chart-render-test fixture: the optional intent controller enabled, with two
# Forge Secrets so the resourceNames list is seen rendering more than one.
intentController:
    enabled: true
    forgeSecrets:
        - patchy-github
        - patchy-github-enterprise
