{{- /* The reply to an approval of a plan that is no longer what the
approver was shown. One line per paragraph: GitHub renders a line break
inside an issue comment as a hard break. */ -}}
{{ .Marker }}
The approval of plan r{{ .PlanRevision }} by {{ code .Actor }}{{ with .Label }} (the {{ code . }} label){{ end }} was refused: {{ if .PlanChanged }}the plan comment was edited after patchy posted it{{ if .IssueChanged }}, and {{ end }}{{ end }}{{ if .IssueChanged }}the issue description changed after the plan was made{{ end }}. An approval covers exactly the plan and the request it was shown with, so patchy will not build from this one{{ if .LabelRemoved }}, and has removed the label{{ end }}.

To go ahead, ask for a new plan: re-apply the {{ code .TriggerLabel }} label or comment {{ .Replan }}, then approve the plan patchy posts.
