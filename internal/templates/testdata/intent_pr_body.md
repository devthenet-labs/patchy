Part of devthenet-labs/intents#1

Add GET /version returning {sha, built} as JSON

---

_patchy built this pull request from plan r2 (`sha256:05126c8813d2`) of the intent, approved by `peter`. Review it like any other change. Merging it completes the intent, and patchy updates the intent issue itself._
